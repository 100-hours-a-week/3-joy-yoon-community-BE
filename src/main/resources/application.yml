jwt:
  secret: "m0R4q7Y2Q4sH9p2P0s1tH3bD9vM7qP1kA4vL0nM8qQ0rC5yX3fS9eW=="
  access-token-expiration: 900000
  refresh-token-expiration: 604800000

server:
  port: 8080

spring:
  # 세션 설정
  session:
    store-type: none # 개발환경: none, 프로덕션: redis (Redis 의존성 필요)
    timeout: 30m
  # 쿠키 설정
  servlet:
    session:
      cookie:
        name: JSESSIONID
        http-only: true
        secure: false # 프로덕션에서는 true (HTTPS)
        same-site: lax # CSRF 방지
        max-age: 30m
        path: /

  thymeleaf:
    prefix: classpath:templates/thymeleaf/
    check-template-location: true
    suffix : .html
    mode: HTML
    cache: false # default true, 개발 시에는 false로 두는 것이 좋음
    enabled: true
  datasource:
    url: jdbc:mysql://localhost:3306/COMMUNITY_DB?serverTimezone=UTC&useSSL=false
    username: root
    password: kakaotech!234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none   # ?? ??? create / create-drop / update ? ??
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

logging:
  level:
    org.hibernate.SQL: debug